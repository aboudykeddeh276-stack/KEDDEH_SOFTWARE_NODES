# Cascading Seed Formula

Object ID: KEX-THEOREM-HEX-0001
Parent: KEX-LEDGER-INV-0001
Evidence: E1
Status: active draft

## Formula

```text
KEX = HEX × HEX
HEX × HEX = KEX
```

## Repository Meaning

The formula defines how the system evolves without losing context.

```text
HEX_A = preserved inventory state
HEX_B = new input or expansion
KEX = resolved new object
```

The result is not a replacement. It is a registered continuation.

## Cascade

```text
KEX_0 = seed repository
KEX_1 = inventory authority
KEX_2 = learning synthesis
KEX_3 = capability architecture
KEX_4 = source seed implementation
```

## System Rule

Every expansion must carry:

- identity
- parent
- evidence
- boundary
- path
- maturity
- dependencies

## Capability Consequence

The software becomes progressively stronger because each new layer is not just added. It is resolved against the previous layer and then written back into inventory.
